import { legacy_createStore as createStore} from 'redux'
const productRedecer=()=>{

}
const rootReducer = ({
    product: productRedecer
})
const store = createStore(rootReducer);
export default store;